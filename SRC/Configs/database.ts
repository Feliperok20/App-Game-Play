const DATABASE_NAME = '@gameplay';

//@app:colection

const COLLECTION_USERS = `${DATABASE_NAME}:user`;
const COLLECTION_APPOINTMENT = `${DATABASE_NAME}:apointments`;

export {
    COLLECTION_USERS,
    COLLECTION_APPOINTMENT,
}